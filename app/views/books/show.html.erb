<p><% @books.each do |book| %><p>
	<%= book.title %>
	<%= book.author %>
	<%= book.genre %>
	<%= book.no_copies %>
	<%= book.avail_for_issue %>
	<%= button_to "Issue Book", issue_path(id: book), :class => "button medium", :method => :patch %>
<% end %>
